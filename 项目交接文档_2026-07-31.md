# 老年脓毒症不良预后多任务建模项目交接文档

更新时间：2026-07-31

## 1. 项目目标

本项目围绕目标论文中的图一流程搭建原型：

1. 使用 MIMIC-IV 老年脓毒症 ICU 窗口数据，构建结构化三源输入：实验室检查、生命体征、治疗措施。
2. 使用 TAMF/图二源码风格训练一阶段多源时序编码器。
3. 将一阶段 EHR 编码器、静态特征、LLM/多智能体推理文本编码器接入图一式整体框架。
4. 当前预测任务已简化为 12、24、48 小时死亡风险三分类头，即 `death_12`、`death_24`、`death_48`。
5. 当前 RAG 处于关闭状态，LLM 输入以结构化 24 小时序列、正常参考范围、history-only 病历文书为主。
6. 多智能体路线参考 ColaCare 式结构：3 个 doctor agent 分别处理不同信息子集，再由 meta agent 汇总。

## 2. 当前可用状态

当前已经具备：

- 数据预处理后的紧凑数据集。
- history-only 病历文书抽取结果。
- LLM 输入用的动态数据，已经完成前向填补、后向填补、中位数填补，并补入 BNP。
- 一阶段 TAMF 编码器权重。
- 图一整体训练脚本和推理脚本。
- 单 LLM 与多 agent 的 DeepSeek 5 例展示日志。

当前没有完全完成：

- 大规模真实 LLM 推理文本数据集尚未全量生成。
- 图一整体融合层和任务头仍需基于完整训练集正式训练。
- RAG 当前关闭；知识库目录存在，但不是当前主流程依赖。
- 影像输入已按需求关闭。

## 3. 推荐运行环境

建议使用 Python 3.9+ 或 Conda 虚拟环境运行。进入项目根目录后执行后续命令，文档中的路径均以项目根目录为基准。

DeepSeek API Key 不要写入代码文件，应通过环境变量传入。PowerShell 示例：

```powershell
$env:DEEPSEEK_API_KEY='your_api_key'
```

## 4. 关键目录结构

```text
program/
  configs/
    dataset_compact_los1_7_year1119.yaml      主配置

  data/
    processed_dataset_outputs_compact_los1_7_year1119/  训练用紧凑窗口数据
    reasoning/                                LLM/agent 输入、记忆、推理文本相关数据
    multimodal/                               病历文书、影像清单等多模态辅助数据

  knowledge_base/
    global_docs/                              知识库文本，当前 RAG 关闭
    docs/

  log/
    deepseek_llm_5_original_preprocess/       单 LLM 5 例展示输出
    deepseek_multiagent_5_original_preprocess_no_memory/ 多 agent 5 例展示输出

  models/
    encoders/multimodal_encoder/weights/      TAMF/EHR 编码器权重
    encoders/text_encoder/                    BioClinicalBERT 等文本编码器缓存
    checkpoints/trained_pipeline/             图一整体模型 checkpoint
    fusion_heads/weights/                     融合层与预测头权重拆分

  scripts/
    data/                                     数据处理脚本
    framework/                                图一框架核心代码
    train/                                    训练入口
    infer/                                    推理入口
    tools/                                    LLM/agent 实验、权重抽取等工具

  vendor/
    TAMF/deep_learning/                       图二/TAMF 源码与适配训练入口
    CheXzero/                                 影像编码器相关，当前不用

  修改下/                                    每次关键改动记录
```

## 5. 主配置说明

主配置文件：

```text
configs/dataset_compact_los1_7_year1119.yaml
```

重要配置：

- `data.processed_dir`: 当前训练窗口数据目录。
- `data.llm_dynamic_csv`: LLM 输入用动态数据，已补 BNP 和填补缺失。
- `data.llm_static_csv`: LLM 输入用静态数据。
- `data.note_csv`: history-only 病历文书。
- `rag.enabled`: 当前为 `false`。
- `model.ehr_encoder_weights`: 当前接入的一阶段 TAMF 编码器权重。
- `model.note_text_encoder`: 当前为 `empty`，原始病历文书只给 LLM/agent，不再直接进图一模型编码。
- `model.reasoning_text_encoder`: 当前为 `bioclinicalbert`，用于编码 LLM/agent 生成的推理文本。
- `model.image_encoder`: 当前为 `empty`，影像输入关闭。
- `model.tasks.binary`: 当前任务为 `death_12`、`death_24`、`death_48`。
- `model.tasks.regression`: 当前为空。

注意：配置中的 `rag.literature_dir` 目前显示乱码，但由于 `rag.enabled=false`，不影响当前主流程。后续若重新开启 RAG，应先修正该路径。

## 6. 数据处理流程

### 6.1 构建三元组/窗口数据

主要脚本：

```text
scripts/data/build_triples.py
scripts/data/create_stage_splits.py
```

当前已经有处理好的数据：

```text
data/processed_dataset_outputs_compact_los1_7_year1119/
```

数据筛选要求：

- 年龄：65 岁及以上。
- ICU LOS：1 到 7 天。
- 年份：2011-2019 对应配置中的 anchor year group。
- 24 小时窗口，步长 4 小时。
- 每个 stay 最多 8 个窗口。

### 6.2 LLM 输入动态数据预处理

主要脚本：

```text
scripts/data/build_llm_imputed_dynamic.py
```

运行：

```powershell
python scripts\data\build_llm_imputed_dynamic.py `
  --config configs/dataset_compact_los1_7_year1119.yaml `
  --mode original_flow
```

输出：

```text
data/reasoning/mimic_dynamic_llm_imputed.csv
data/reasoning/mimic_static_llm_preprocessed.csv
data/reasoning/mimic_dynamic_llm_imputed_summary.json
```

说明：

- 使用原 4.0 preprocess 思路：异常值置 NaN、同一 stay 内前向填补、后向填补、全局中位数填补。
- BNP 原始存在于 `../mimiciv/mimiciv/lab_test.csv`，masked lab 中没有，因此脚本会按 `stay_id + hr` 补入 BNP。
- 最近检查：BNP 已进入输出动态数据，`tracked_after_missing.bnp=0`。

### 6.3 病历文书处理

主要脚本：

```text
scripts/data/build_history_only_notes.py
```

输出：

```text
data/multimodal/history_only_notes.csv
```

当前策略：抽取 admission/history 相关字段，作为 LLM/agent 输入；未对病历按小时做时序化。

## 7. 一阶段 TAMF 编码器训练

当前使用 vendor/TAMF/deep_learning 源码风格，适配处理好的 TripleWindowDataset。

训练脚本：

```text
scripts/train/train_stage1_tamf_split.py
```

示例命令：

```powershell
python scripts\train\train_stage1_tamf_split.py `
  --config configs/dataset_compact_los1_7_year1119.yaml `
  --save-dir vendor/TAMF/deep_learning/checkpoints/stage1_compact_local `
  --epochs 10 `
  --batch-size 32 `
  --num-workers 4 `
  --pin-memory `
  --embed-dim 128 `
  --num-heads 4
```

当前接入权重：

```text
models/encoders/multimodal_encoder/weights/stage1_compact_local_death48_best_test_loss_encoder_only.pt
```

该权重用于图一模块一 EHR/多源时序编码器。

## 8. 图一整体训练

训练脚本：

```text
scripts/train/train.py
```

示例命令：

```powershell
python scripts\train\train.py `
  --config configs/dataset_compact_los1_7_year1119.yaml `
  --epochs 2 `
  --batch-size 8 `
  --max-samples 512 `
  --checkpoint-path models/checkpoints/trained_pipeline/latest.pt
```

训练输入：

- EHR 三源张量：lab、vital、treatment。
- 静态特征：当前 5 个，年龄、性别、BMI、CCI、GNRI。
- 推理文本：来自 `data/reasoning/reasoning_texts.csv`，若没有则脚本会用 placeholder。
- 病历文书：当前不直接作为图一模型文本分支输入，只进入 LLM/agent 生成推理文本。
- 影像：关闭。

训练损失：

- 三个二分类任务的 BCEWithLogitsLoss。
- CARER 风格 EHR/推理文本 alignment loss。
- 当前没有回归任务损失。

## 9. 图一整体推理

推理脚本：

```text
scripts/infer/predict.py
```

示例命令：

```powershell
python scripts\infer\predict.py `
  --config configs/dataset_compact_los1_7_year1119.yaml `
  --checkpoint-path models/checkpoints/trained_pipeline/latest.pt `
  --split test `
  --max-samples 128 `
  --output-csv outputs/predictions.csv
```

注意：旧的 `predictions/` 目录是早期 smoke/verify 输出，已经不建议继续使用。后续建议统一写到 `outputs/` 或新的实验目录。

## 10. 单 LLM 与多 Agent 推理展示

### 10.1 单 LLM 5 例

脚本：

```text
scripts/tools/run_nine_model_rag_eval.py
```

示例命令：

```powershell
$env:DEEPSEEK_API_KEY='your_api_key'
$env:PYTHONIOENCODING='utf-8'
python scripts\tools\run_nine_model_rag_eval.py `
  --config configs/dataset_compact_los1_7_year1119.yaml `
  --samples-jsonl reasoning/sampled_20_cases.jsonl `
  --max-samples 5 `
  --providers DeepSeek `
  --model-ids deepseek-v4-flash `
  --log-dir log/deepseek_llm_5_original_preprocess
```

输出目录：

```text
log/deepseek_llm_5_original_preprocess/
```

### 10.2 多 Agent 5 例

脚本：

```text
scripts/tools/run_multiagent_flash_compare.py
```

示例命令：

```powershell
$env:DEEPSEEK_API_KEY='your_api_key'
$env:PYTHONIOENCODING='utf-8'
$env:AGENTIC_CHAT_MAX_ATTEMPTS='2'
$env:AGENTIC_CHAT_RETRY_WAIT_SECONDS='20'
$env:AGENTIC_CHAT_TIMEOUT_SECONDS='120'
$env:AGENTIC_CHAT_THROTTLE_SECONDS='1'
python scripts\tools\run_multiagent_flash_compare.py `
  --config configs/dataset_compact_los1_7_year1119.yaml `
  --samples-jsonl reasoning/sampled_20_cases.jsonl `
  --max-samples 5 `
  --include-deepseek `
  --skip-glm `
  --disable-memory `
  --max-consult-rounds 1 `
  --request-note-limit 2400 `
  --log-dir log/deepseek_multiagent_5_original_preprocess_no_memory
```

输出目录结构：

```text
log/deepseek_multiagent_5_original_preprocess_no_memory/
  structured_patient_inputs/     每条样本原始结构化输入
  agent_context_inputs/          agent 上下文输入
  doctor_inputs/{sample_id}/     doctor_1/2/3 三个不同输入
  doctor_outputs/{sample_id}/    doctor_1/2/3 三个不同输出
  meta_agent_inputs/             meta agent 汇总输入
  meta_agent_outputs/            meta agent 汇总输出
  sample_traces/                 单条样本完整数据流
  readable_outputs/              Markdown 可读版
  outputs/                       JSONL 总输出
```

## 11. Prompt 构建逻辑

### 单 LLM

代码：

```text
scripts/framework/reasoning/prompts.py
```

单 LLM user prompt 主体为：

```text
User Prompt:
Please process the following patient data:
### 1. Static Structured Data
...
### 2. Dynamic Time-Series Data
...
### 3. Raw Admission Record Text
...
```

注意：当前代码中为了接入 BNP，在 Creatinine 后加入了：

```text
- BNP (pg/mL): {bnp_list}
```

如果后续需要严格还原用户最新给出的模板，应决定是否保留 BNP 行；BNP 是最近明确要求加入的数据项。

### 多 Agent

代码：

```text
scripts/framework/reasoning/agentic_reasoner.py
scripts/framework/reasoning/prompts.py
```

当前 doctor 分工：

- doctor_1：血流动力学和呼吸，关注 heart_rate、blood_pressure、spo2、vasopressors、ventilation。
- doctor_2：器官功能和实验室，关注 glucose、creatinine、bnp、platelet、neutrophils、lymphocytes。
- doctor_3：病史、治疗和整体状态，关注 age、gender、bmi、cci_score、gnri、vasopressors、ventilation、病历文书。

Meta agent 输入：完整 patient summary + 三个 doctor 输出 + RAG 状态。

## 12. 已清理内容

已经删除：

- `scripts/infer/test_langgraph_deepseek.py`
- `scripts/train/smoke_test_model.py`
- `scripts/infer/compare_one_case_agent_vs_single.py`
- `scripts/infer/run_figure1_rag_llm_flow.py`
- `scripts/data/build_synthetic_multimodal_assets.py`
- `scripts/tools/compare_llm_reasoning_outputs.py`
- `docs/项目交接文档.md`
- `CLAUDE.md`
- `predictions/`
- `log_archive/`
- `llm_reasoning/` 与 `llm_reasoning.zip`
- `pdf_pages/`
- `.pip_cache/`
- `tmp_*.pdf`、`tmp_*.txt`
- 各级 `__pycache__/`
- `log/*.zip`
- `models/checkpoints/archive/`
- `models/checkpoints/trained_pipeline/` 下早期 `smoke`、`local_test`、`tamf_upstream_test` 等试跑 checkpoint
- `models/encoders/*/weights/` 下早期 `smoke`、`local_test`、`tamf_upstream_test`、`alignment`、`carer`、`dataset_output`、`fixed_schema` 等拆分试跑权重
- `models/fusion_heads/weights/` 下早期 `smoke`、`local_test`、`tamf_upstream_test`、`alignment`、`carer`、`dataset_output`、`fixed_schema` 等融合层/预测头拆分试跑权重
- 删除后变空的影像 smoke 权重目录和空的 CheXzero 占位目录
- `data_preprocessing_package_20260716_excluded_llm_code/cache_files/`

这些是早期临时测试入口、旧输出、旧归档、缓存、重复文档或旧试跑权重，不属于当前主流程。

## 13. 仍可继续人工确认的清理项

以下内容本次未删除，建议后续按交付需要人工确认：

- `data_preprocessing_package_20260716/`：早期打包的数据预处理代码副本；如原始数据说明目录已单独保存，可考虑移出项目。
- `data_preprocessing_package_20260716_excluded_llm_code/`：早期剔除 LLM 代码后的预处理包副本；如不再交付预处理包，可考虑移出项目。
- `vendor/EMERGE/`、`vendor/KARE/`、`vendor/CheXzero/`：参考仓库或影像/知识增强参考代码，当前主流程不直接依赖；如只交付当前可运行主线，可考虑单独归档。
- `修改/`：当前为空目录，删除时被本地安全策略拦截；不影响主流程，可手动删除。

保守起见，不建议删除：

- `data/`
- `models/`
- `vendor/`
- `configs/`
- `scripts/framework/`
- `scripts/data/`
- `scripts/train/train.py`
- `scripts/train/train_stage1_tamf_split.py`
- `scripts/infer/predict.py`
- `scripts/tools/run_nine_model_rag_eval.py`
- `scripts/tools/run_multiagent_flash_compare.py`
- `models/checkpoints/trained_pipeline/latest.pt`
- `models/fusion_heads/weights/latest_*`

## 14. 后续建议

1. 先确认单 LLM prompt 是否保留 BNP 行。如果严格按最新手写 prompt，可删除 BNP；如果按最新数据要求，应保留 BNP。
2. 生成全量或抽样的 LLM/agent 推理文本，写入 `data/reasoning/reasoning_texts.csv`。
3. 用正式推理文本重新训练图一整体模型。
4. 在测试集上评估 `death_12`、`death_24`、`death_48` 的 AUROC、AUPRC、准确率、召回率等。
5. 如重新启用 RAG，先修复配置中的乱码路径，并明确知识库来源。
6. 如需要恢复 memory，自进化策略建议先使用 `error_only`，避免每条样本都写记忆导致噪声累积。
